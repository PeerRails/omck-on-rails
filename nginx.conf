user www-data;
worker_processes auto;
pid /run/nginx.pid;

#docker run --name nginx -p 80:80 -v /git/omck-on-rails/nginx.conf:/etc/nginx/nginx.conf:ro -v /git/omck-on-rails:/var/www/app/current:ro -v /data:/data:ro -v /data/logs/nginx:/var/log/nginx -d nginx

events {
    worker_connections  768;
}

http {

    include       mime.types;
    default_type  application/octet-stream;
    types_hash_max_size 4096;

    access_log  /var/log/nginx/access.log;
    error_log  /var/log/nginx/error.log;
    sendfile        on;
    keepalive_timeout  65;

    gzip  on;

    server {
        listen       80;
        # Check this
        server_name  localhost;
        root /var/www/app/current/public;

        location / {
            proxy_pass http://172.17.42.1:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        location /irc {
        }

        location /player {
        }

        location /download {
          root /data/videos;
          autoindex on;
        }

        location /hls {
            types {
                application/vnd.apple.mpegurl m3u8;
            }
            add_header Cache-Control no-cache;
            add_header Access-Control-Allow-Origin *;
            root /data;
        }

        location /dash {
            types {
              application/dash+xml mpd;
            }
            add_header Cache-Control no-cache;
            add_header Access-Control-Allow-Origin *;
            root /data;
        }

        location /videos {
          root /data;
        }

        location /language {
          root /var/www/app/current/public/irc;
        }
    }
}
