require 'rails_helper'

RSpec.describe Session, type: :model do
  describe "associations" do
      it "should belong to client"
      it "should have unique session_id"
      it "should give an interface to create session"
      it "should give an interface destroy session"
  end

  describe "interfaces" do
      it "should give an interface to create session"
      it "should give an interface destroy session"
  end
end
